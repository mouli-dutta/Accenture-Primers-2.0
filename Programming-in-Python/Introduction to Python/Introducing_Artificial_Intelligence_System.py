name = input('Enter the name: ')
creator = input('Enter the creator name: ')
purpose = input('Enter the purpose: ')
size = input('Enter the memory size: ')
speed = input('Enter the speed: ')

print('My Details:\n')

print(f'I am {name}, created by {creator}, for the purpose of {purpose}.\n')

print(f'Memory I consume is around {size} and my speed is {speed} GHz.')
